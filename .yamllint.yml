extends: default

ignore: |
  .venv/
  .collections/
  **/.collections/
  **/__pycache__/
  .git/

rules:
  document-start: disable
  truthy:
    allowed-values: ["true", "false", "yes", "no"]
  line-length:
    max: 120
    level: warning
